# Evasion

## Static
- Bản chất vấn đề detection: search for data in a file (or buffer) -> blacklisting
- Search: hash matching (full file /range in file) => ioc

=> evasion: sửa data, add junk code, ...
- Fuzzy Hash, import hash

=> Basic encoder, detect encoded string
- string matching & Pattern matching: regex & aho-corrasick

=> Obfuscation, junk code, file loader 101
- file structure and metadata (and embedded file, arcive files, ...)


## dynamic
- function hooking, debugging
- Emulator

## Heuristic & emulator

# Antivirus the concept

## rootkit
- Userland rootkit
- Kernel land rootkit
